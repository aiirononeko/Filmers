<h1>個展一覧</h1>

<%= link_to '個展を開催する', new_photo_path %>

<% @photos.each do |photo| %>
  <div>
    <%= link_to photo.title, photo_path(photo) %>
    <%= image_tag photo.image if photo.image.attached? %>
    <%= link_to '編集', edit_photo_path(photo) %>
  </div>
<% end %>