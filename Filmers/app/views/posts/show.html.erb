<h1><%= @post.title %></h1>
<p><%= @post.body %></p>
<p><%= @post.created_at.strftime('%Y年 %m月 %d日') %></p>

<%= link_to '編集', edit_post_path(@post) %>
<%= link_to '削除', post_path(@post), method: :delete, data: { confirm: "#{@post.title}を削除します。本当によろしいですか？" } %>